#!/usr/bin/env bash

if [[ $1 == "--build" ]]; then
	$SCRIPTS/docs-build
else
	(
		trap 'kill 0' SIGINT;
		SHELL=/bin/bash chokidar README.ts -c $SCRIPTS/docs-build &
		markserv README.md --address 0.0.0.0 --port 3001 --livereloadport 3002
	)
fi
